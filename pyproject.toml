[project]
name = "ppap"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pyminizip>=0.2.6",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.21.0",
]
